<%= form_with(model: tiny_url, local: true) do |form| %>
  <% if tiny_url.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tiny_url.errors.count, "error") %> prohibited this tiny_url from being saved:</h2>

      <ul>
      <% tiny_url.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
